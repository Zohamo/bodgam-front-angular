import { ProfileRepresentation } from './profile-representation.model';

describe('ProfileRepresentation', () => {
  it('should create an instance', () => {
    expect(new ProfileRepresentation()).toBeTruthy();
  });
});
