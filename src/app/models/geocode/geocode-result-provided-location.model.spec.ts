import { GeocodeResultProvidedLocation } from './geocode-result-provided-location.model';

describe('GeocodeResultProvidedLocation', () => {
  it('should create an instance', () => {
    expect(new GeocodeResultProvidedLocation()).toBeTruthy();
  });
});
