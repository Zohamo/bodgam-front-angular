import { EventSubscriptionGroups } from './event-subscription-groups.model';

describe('EventSubscriptionGroups', () => {
  it('should create an instance', () => {
    expect(new EventSubscriptionGroups()).toBeTruthy();
  });
});
