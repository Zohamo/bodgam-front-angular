import { NotificationEmail } from './notification-email.model';

describe('NotificationEmail', () => {
  it('should create an instance', () => {
    expect(new NotificationEmail()).toBeTruthy();
  });
});
