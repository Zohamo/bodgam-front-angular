import { NotificationEmailContentItem } from './notification-email-content-item.model';

describe('NotificationEmailContentItem', () => {
  it('should create an instance', () => {
    expect(new NotificationEmailContentItem()).toBeTruthy();
  });
});
