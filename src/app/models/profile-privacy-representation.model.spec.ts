import { ProfilePrivacyRepresentation } from './profile-privacy-representation.model';

describe('ProfilePrivacyRepresentation', () => {
  it('should create an instance', () => {
    expect(new ProfilePrivacyRepresentation()).toBeTruthy();
  });
});
