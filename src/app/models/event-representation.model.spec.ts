import { EventRepresentation } from './event-representation.model';

describe('EventRepresentation', () => {
  it('should create an instance', () => {
    expect(new EventRepresentation()).toBeTruthy();
  });
});
