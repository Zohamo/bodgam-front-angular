import { ProfileRatingsRepresentation } from './profile-ratings-representation.model';

describe('ProfileRatingsRepresentation', () => {
  it('should create an instance', () => {
    expect(new ProfileRatingsRepresentation()).toBeTruthy();
  });
});
