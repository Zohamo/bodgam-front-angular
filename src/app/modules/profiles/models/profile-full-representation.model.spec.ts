import { ProfileFullRepresentation } from './profile-full-representation.model';

describe('ProfileFullRepresentation', () => {
  it('should create an instance', () => {
    expect(new ProfileFullRepresentation()).toBeTruthy();
  });
});
