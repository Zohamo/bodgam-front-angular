import { NavigatorGeolocationPosition } from './navigator-geolocation-position.model';

describe('NavigatorGeolocationPosition', () => {
  it('should create an instance', () => {
    expect(new NavigatorGeolocationPosition()).toBeTruthy();
  });
});
