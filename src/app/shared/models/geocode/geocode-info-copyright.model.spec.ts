import { GeocodeInfoCopyright } from './geocode-info-copyright.model';

describe('GeocodeInfoCopyright', () => {
  it('should create an instance', () => {
    expect(new GeocodeInfoCopyright()).toBeTruthy();
  });
});
