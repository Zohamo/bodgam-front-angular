import { GeocodeResultLocation } from './geocode-result-location.model';

describe('GeocodeResultLocation', () => {
  it('should create an instance', () => {
    expect(new GeocodeResultLocation()).toBeTruthy();
  });
});
