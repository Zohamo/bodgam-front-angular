import { UserPrivacyRepresentation } from './user-privacy-representation.model';

describe('UserPrivacyRepresentation', () => {
  it('should create an instance', () => {
    expect(new UserPrivacyRepresentation()).toBeTruthy();
  });
});
