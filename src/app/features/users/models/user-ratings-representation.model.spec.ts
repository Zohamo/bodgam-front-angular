import { UserRatingsRepresentation } from './user-ratings-representation.model';

describe('UserRatingsRepresentation', () => {
  it('should create an instance', () => {
    expect(new UserRatingsRepresentation()).toBeTruthy();
  });
});
