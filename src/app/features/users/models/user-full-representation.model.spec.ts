import { UserFullRepresentation } from './user-full-representation.model';

describe('UserFullRepresentation', () => {
  it('should create an instance', () => {
    expect(new UserFullRepresentation()).toBeTruthy();
  });
});
