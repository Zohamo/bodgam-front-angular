import { LocationRepresentation } from './location-representation.model';

describe('LocationRepresentation', () => {
  it('should create an instance', () => {
    expect(new LocationRepresentation()).toBeTruthy();
  });
});
